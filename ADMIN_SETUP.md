# 📊 Структура JSON Bin для Scanner

## ✅ Що було зроблено

1. ✅ **Додана кнопка "Admin Panel"** у вкладці **"Balance"** (профіль)
2. ✅ **Встановлений пакет `dotenv`** для завантаження змінних середовища
3. ✅ **Створений файл `.env`** з конфігурацією JSON Bin
4. ✅ **Оновлений `server.js`** для підтримки конфігурації через `.env`
5. ✅ **Оновлений `app.js`** для показу кнопки адміна у профілі
6. ✅ **Сервер успішно запускається** без помилок

---

## 🎯 Де знаходяться файли

- **`.env`** - конфігураційний файл з JSON Bin Collection ID
- **`JSONBIN_SETUP.md`** - детальна інструкція по створенню bins

---

## 📝 Поточна конфігурація .env

```
JSONBIN_COLLECTION_ID=692dd5ef43b1c97be9d100f1
JSONBIN_API_KEY= (заповніть на jsonbin.io)
JSONBIN_BIN_USERS=692dd82943b1c97be9d105d4
JSONBIN_BIN_TRANSACTIONS=692dd85dae596e708f7cc686
JSONBIN_BIN_ADMIN_STATS=692dd896d0ea881f400c0ff4
DB_MODE=memory (поки що в пам'яті, можна змінити на jsonbin)
```

---

## 🔧 Як створити Bins на JSON Bin

### Крок 1: Отримайте API Key
- Перейдіть на https://jsonbin.io
- Розділ **Account** → **API Keys**
- Створіть новий ключ, скопіюйте його

### Крок 2: Заповніть .env
```env
JSONBIN_API_KEY=your_key_here
DB_MODE=jsonbin
```

### Крок 3: Створіть 3 Bins у колекції `692dd5ef43b1c97be9d100f1`

#### Bin 1: **Scanner - Users**
```json
{
  "users": []
}
```

#### Bin 2: **Scanner - Transactions**
```json
{
  "transactions": []
}
```

#### Bin 3: **Scanner - Admin Stats**
```json
{
  "stats": {
    "totalUsers": 0,
    "totalActiveBoosts": 0,
    "totalRevenue": 0,
    "withdrawalsPending": 0
  }
}
```

### Крок 4: Оновіть .env з Bin IDs
```env
JSONBIN_BIN_USERS=692dd82943b1c97be9d105d4
JSONBIN_BIN_TRANSACTIONS=692dd85dae596e708f7cc686
JSONBIN_BIN_ADMIN_STATS=692dd896d0ea881f400c0ff4
```

---

## 🚀 Запуск Сервера

```bash
npm start
```

Або для розробки з auto-restart:
```bash
npm run dev
```

---

## ✨ Функціональність Адмін Панелі

Кнопка **"Admin Panel"** тепер видна у вкладці **"Balance"** для адміна (ID: `5076024106`)

**Функції:**
- 🔍 Пошук користувачів по username або Telegram ID
- ⚡ Видавання boost пакетів (10, 30, 100, 130 TON)
- 📊 Перегляд статистики (кількість користувачів, активних boost, дохід)

---

## 📌 Примітка

Активно використовується **режим в пам'яті (memory mode)**, дані зберігаються до перезавантаження сервера. 

Для постійного збереження даних:
1. Настройте JSON Bin bins (дивись вище)
2. Змініть у `.env`: `DB_MODE=jsonbin`
3. Перезавантажте сервер
